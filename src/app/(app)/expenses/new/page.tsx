import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewExpensePage() {
  return <ComingSoon title="Add Expense" description="Expense entry form — API route POST /api/expenses is ready. UI form is next." />;
}
