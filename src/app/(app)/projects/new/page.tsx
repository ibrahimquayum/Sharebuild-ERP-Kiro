import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewProjectPage() {
  return <ComingSoon title="New Project" description="Project creation form — API route POST /api/projects is ready. UI form is next." />;
}
