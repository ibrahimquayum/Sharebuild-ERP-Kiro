import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewPayablePage() {
  return <ComingSoon title="Record Supplier Bill" description="Supplier bill/payable entry form — SupplierPayable model is in the schema. UI form is next." />;
}
