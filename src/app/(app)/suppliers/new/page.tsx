import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewSupplierPage() {
  return <ComingSoon title="Add Supplier" description="Supplier registration form — API route POST /api/suppliers is ready. UI form is next." />;
}
