import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewPhasePage() {
  return <ComingSoon title="Add Phase" description="Phase creation form — API route POST /api/phases is ready. UI form is next." />;
}
