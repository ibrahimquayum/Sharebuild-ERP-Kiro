import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewMaterialPage() {
  return <ComingSoon title="Add Material Purchase" description="Material purchase entry form — schema and API ready. UI form is next." />;
}
