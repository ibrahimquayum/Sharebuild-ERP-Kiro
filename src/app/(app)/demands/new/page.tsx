import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewDemandPage() {
  return <ComingSoon title="Issue Demand Notice" description="Demand notice form — Demand model is in the schema. UI form is next." />;
}
