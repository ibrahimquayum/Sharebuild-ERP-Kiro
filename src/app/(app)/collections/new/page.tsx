import { ComingSoon } from '@/components/shared/coming-soon';
export default function NewCollectionPage() {
  return <ComingSoon title="Record Payment" description="Payment collection form — API route POST /api/collections is ready. UI form is next." />;
}
